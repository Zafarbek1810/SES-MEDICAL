
  # Enhance Detail Orientation

  This is a code bundle for Enhance Detail Orientation. The original project is available at https://www.figma.com/design/snmWH1o112xC7GvejBWBuO/Enhance-Detail-Orientation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  